#volumes:
#  pg_data:
#  static:
#  media:
#
#services:
#  db:
#    image: postgres:13
#    env_file: .env
#    volumes:
#      - pg_data:/var/lib/postgresql/data
#  backend:
#    image: ilyushkinss/foodgram_backend
#    env_file: .env
#    volumes:
#      - static:/backend_static/
#      - media:/media/
#    depends_on:
#      - db
#  frontend:
#    image: ilyushkinss/foodgram_frontend
#    command: cp -r /app/build/. /static/
#    volumes:
#      - static:/static/
#  gateway:
#    image: ilyushkinss/foodgram_gateway
#    env_file: .env
#    ports:
#      - 8080:80
#    volumes:
#      - static:/static/
#      - media:/media/
#      - ../frontend/build:/usr/share/nginx/html/
#      - ./docs/:/usr/share/nginx/html/api/docs/
#    depends_on:
#      - backend
#      - frontend
version: '3.8'

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    image: ilyushkinss/foodgram_backend
    env_file: .env
    volumes:
      - static:/backend_static/
      - media:/media/
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    command: >
      sh -c "while ! nc -z db 5432; do
               echo 'Waiting for PostgreSQL...';
               sleep 2;
             done &&
             python manage.py migrate &&
             python manage.py collectstatic --no-input &&
             gunicorn foodgram.wsgi:application --bind 0.0.0.0:8000"

  frontend:
    image: ilyushkinss/foodgram_frontend
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static/

  gateway:
    image: ilyushkinss/foodgram_gateway
    env_file: .env
    ports:
      - 8080:80
    volumes:
      - static:/static/
      - media:/media/
      - ../frontend/build:/usr/share/nginx/html/
      - ./docs/:/usr/share/nginx/html/api/docs/
    depends_on:
      - backend
      - frontend
